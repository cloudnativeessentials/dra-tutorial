apiVersion: v1
kind: Pod
metadata:
  name: ollama
  namespace: dra-tutorial
  labels:
    app: pod
spec:
  containers:
  - name: ollama
    image: docker.io/ollama/ollama:0.3.9
    resources:
      claims:
      - name: gpu
    # requests:
      #  nvidia.com/gpu: "1"
  # tolerations:
  # - key: nvidia.com/gpu
    # effect: NoSchedule
    # operator: Exists
  resourceClaims:
  - name: gpu
    resourceClaimName: example-gpu